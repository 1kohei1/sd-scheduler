import * as React from 'react';

export interface ScheduleProps {
}

export default class Schedule extends React.Component<ScheduleProps, any> {
  render() {
    return (
      <div>
        This is a schedule component
      </div>
    );
  }
}
